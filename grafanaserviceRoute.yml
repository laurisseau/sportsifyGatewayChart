apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  namespace: monitoring            # HTTPRoute lives in Grafana namespace
spec:
  parentRefs:
  - name: sportsify-gateway
    namespace: sportsify-ns
    sectionName: http
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /grafana
    backendRefs:
    - name: monitoring
      namespace: monitoring        # points to Grafana service namespace
      port: 80
      kind: Service